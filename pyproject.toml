[project]
name = "my-bitbucket-pipe"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bitbucket-pipes-toolkit==6.2.0",
    "requests==2.19.1",
    
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"